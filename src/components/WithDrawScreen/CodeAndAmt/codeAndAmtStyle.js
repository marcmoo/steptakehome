import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  upperContainerStyle: {
    marginVertical: 20,
  },

  contentTextStyle: {
    paddingVertical: 10,
    fontSize: 24,
    textAlign: 'center',
  },
});

export default styles;
